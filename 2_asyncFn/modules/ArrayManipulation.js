/***
 * --- params ---
 * data: [ { name:string, age:number, skills:string[] }, {...}, {...} ]
 * keys: string[]
 * conditions: {condition: string, threshold_value: string | number} ]
 */

class ArrayManipulation {}

module.exports = ArrayManipulation;
